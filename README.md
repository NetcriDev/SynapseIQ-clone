# SynapseIQ

Repository layout

root
├── src
│   ├── watson
│   │   └── chat
│   └── subBranch2
│       └── nothing yet
└── conf
    └── ibm

You can connect to the current VM instance with the following command;

```ssh -i "C:\Users\eperler\work\keys\rel8tedkey01_rsa.prv" root@52.116.202.144

use the following command to execute python scripts

```pyhton3.11